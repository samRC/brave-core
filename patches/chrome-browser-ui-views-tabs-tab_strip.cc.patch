diff --git a/chrome/browser/ui/views/tabs/tab_strip.cc b/chrome/browser/ui/views/tabs/tab_strip.cc
index 5e00e54d3ca0d76453f7ebac4e810e41f50b2801..ab35d6a66f355141f6c5cd648758d996953d93e8 100644
--- a/chrome/browser/ui/views/tabs/tab_strip.cc
+++ b/chrome/browser/ui/views/tabs/tab_strip.cc
@@ -206,6 +206,7 @@ class TabStrip::TabDragContextImpl : public TabDragContext {
            event.type() == ui::ET_GESTURE_SCROLL_BEGIN);
 
     drag_controller_ = std::make_unique<TabDragController>();
+    BRAVE_UI_VIEWS_TAB_TAB_STRIP_INIT_DRAG_CONTROLLER_FOR_VERTICAL_TABS
     drag_controller_->Init(this, source, dragging_views, gfx::Point(x, y),
                            event.x(), std::move(selection_model),
                            EventSourceFromEvent(event));
@@ -417,6 +418,8 @@ class TabStrip::TabDragContextImpl : public TabDragContext {
       const std::vector<TabSlotView*>& views) override {
     DCHECK(!views.empty());
 
+    BRAVE_UI_VIEWS_TABS_TAB_STRIP_CALCULATE_DRAGGED_VIEW_BOUNDS_FOR_VERTICAL_TABS
+
     std::vector<gfx::Rect> bounds;
     const int overlap = TabStyle::GetTabOverlap();
     int x = 0;
@@ -570,6 +573,8 @@ class TabStrip::TabDragContextImpl : public TabDragContext {
         continue;
       }
 
+      BRAVE_UI_VIEWS_TAB_STRIP_CALCULATE_INDEX_FOR_VERTICAL_TABS
+
       // If there's a group header here, and we're dragging a group, we might
       // end up on either side of that header. Check both cases to find the
       // best option.
